<Page x:Class="TheToyRoomDesktop.Views.CollectionPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="My Collection"
      Background="#f8f9fa">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Title -->
        <TextBlock Grid.Row="0" Text="My Collection" FontSize="32" FontWeight="Bold" Foreground="#6f42c1" Margin="0,0,0,20"/>
        
        <!-- Search and Filter Bar -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBox x:Name="SearchBox" 
                     Grid.Column="0"
                     Padding="10"
                     FontSize="14"
                     VerticalContentAlignment="Center"
                     TextChanged="SearchBox_TextChanged">
                <TextBox.Style>
                    <Style TargetType="TextBox">
                        <Style.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <VisualBrush Stretch="None" AlignmentX="Left">
                                            <VisualBrush.Visual>
                                                <TextBlock Text="Search by name, character, manufacturer, or deco..." 
                                                          Foreground="#adb5bd" 
                                                          Margin="5,0,0,0"
                                                          VerticalAlignment="Center"/>
                                            </VisualBrush.Visual>
                                        </VisualBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </TextBox.Style>
            </TextBox>
            
            <Button Grid.Column="1" Content="Refresh" Click="Refresh_Click" Margin="10,0,0,0" Padding="15,8"/>
        </Grid>
        
        <!-- Data Grid -->
        <DataGrid x:Name="CollectionGrid" 
                  Grid.Row="2"
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  Background="White"
                  BorderBrush="#dee2e6"
                  BorderThickness="1"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  RowBackground="White"
                  AlternatingRowBackground="#f8f9fa"
                  CanUserResizeRows="False"
                  CanUserReorderColumns="False"
                  SelectionMode="Single"
                  MouseDoubleClick="CollectionGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Character" Binding="{Binding Character}" Width="*"/>
                <DataGridTextColumn Header="Deco" Binding="{Binding DecoName}" Width="100"/>
                <DataGridTextColumn Header="Manufacturer" Binding="{Binding Manufacturer}" Width="*"/>
                <DataGridCheckBoxColumn Header="Reissue" Binding="{Binding Reissue}" Width="80"/>
                <DataGridCheckBoxColumn Header="Stylized" Binding="{Binding Stylized}" Width="80"/>
                <DataGridTextColumn Header="Original" Binding="{Binding OriginalPrice, StringFormat=C2}" Width="100"/>
                <DataGridTextColumn Header="Current" Binding="{Binding CurrentValue, StringFormat=C2}" Width="100"/>
                <DataGridTemplateColumn Header="Actions" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="View" Click="View_Click" Tag="{Binding Id}" Margin="2" Padding="8,4"/>
                                <Button Content="Edit" Click="Edit_Click" Tag="{Binding Id}" Margin="2" Padding="8,4"/>
                                <Button Content="Delete" Click="Delete_Click" Tag="{Binding Id}" Margin="2" Padding="8,4" Background="#dc3545"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
